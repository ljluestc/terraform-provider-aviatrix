# Terraform Provider Aviatrix - Test Environment Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# Core Test Configuration
# ==============================================================================

# Enable acceptance tests (set to 1 to enable)
TF_ACC=1

# Test execution timeout (default: 30m)
GO_TEST_TIMEOUT=30m

# Test artifact directory for logs and results
TEST_ARTIFACT_DIR=./test-results

# Enable parallel test execution
ENABLE_PARALLEL_TESTS=true

# Enable detailed debug logging
ENABLE_DETAILED_LOGS=false

# Maximum Go processes for parallel tests
GOMAXPROCS=4

# ==============================================================================
# Aviatrix Controller Configuration (REQUIRED for all tests)
# ==============================================================================

AVIATRIX_CONTROLLER_IP=your-controller-ip-or-hostname
AVIATRIX_USERNAME=your-controller-username
AVIATRIX_PASSWORD=your-controller-password

# ==============================================================================
# AWS Configuration
# ==============================================================================

# Set to 'yes' to skip AWS tests
SKIP_ACCOUNT_AWS=no

# AWS credentials (required if AWS tests enabled)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_ACCOUNT_NUMBER=your-aws-account-number
AWS_DEFAULT_REGION=us-east-1

# ==============================================================================
# Azure Configuration
# ==============================================================================

# Set to 'yes' to skip Azure tests
SKIP_ACCOUNT_AZURE=yes

# Azure credentials (required if Azure tests enabled)
ARM_CLIENT_ID=your-azure-client-id
ARM_CLIENT_SECRET=your-azure-client-secret
ARM_SUBSCRIPTION_ID=your-azure-subscription-id
ARM_TENANT_ID=your-azure-tenant-id

# ==============================================================================
# GCP Configuration
# ==============================================================================

# Set to 'yes' to skip GCP tests
SKIP_ACCOUNT_GCP=yes

# GCP credentials (required if GCP tests enabled)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-credentials.json
GOOGLE_PROJECT=your-gcp-project-id
GOOGLE_REGION=us-central1

# ==============================================================================
# OCI (Oracle Cloud Infrastructure) Configuration
# ==============================================================================

# Set to 'yes' to skip OCI tests
SKIP_ACCOUNT_OCI=yes

# OCI credentials (required if OCI tests enabled)
OCI_USER_ID=your-oci-user-ocid
OCI_TENANCY_ID=your-oci-tenancy-ocid
OCI_FINGERPRINT=your-oci-key-fingerprint
OCI_PRIVATE_KEY_PATH=/path/to/oci-private-key.pem
OCI_REGION=us-ashburn-1

# ==============================================================================
# Go Version Configuration
# ==============================================================================

# Go version for tests (informational)
GO_VERSION=1.23

# Terraform version for tests (informational)
TF_VERSION=1.6.6

# ==============================================================================
# Usage Instructions
# ==============================================================================
#
# 1. Copy this file:
#    cp .env.test.example .env
#
# 2. Fill in your credentials for the cloud providers you want to test
#
# 3. Set SKIP_ACCOUNT_* to 'yes' for providers you don't want to test
#
# 4. Run tests:
#    source .env         # Load environment variables
#    make test           # Run unit tests
#    make testacc        # Run acceptance tests
#
# 5. For Docker-based testing:
#    docker-compose -f docker-compose.test.yml up unit-tests
#
# 6. For CI/CD, set these as GitHub repository secrets
#
# ==============================================================================
# Security Note
# ==============================================================================
#
# NEVER commit the .env file with actual credentials to version control!
# The .env file is already in .gitignore to prevent accidental commits.
#
# ==============================================================================
